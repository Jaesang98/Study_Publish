## 💡 part1-17 pseudo-class로 인터랙티브 버튼 만들기

### 🔹 pseudo-class
- 상태에 따라서 스타일을 줄 수 있음

### 🔹 스타일링
- `hover` : 마우스 갖다댔을때
- `active` : 클릭했을때
- `focus` : 포커스됬을 때 (인풋 클릭 시 테두리 변할 때 사용)
- 순서는 h f a 순으로 적기
- 방문적 링크와 방문 후 링크는 `link, visited` 순으로 적용